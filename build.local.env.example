# Optional local build settings for build-and-install.sh.
# Copy this file to build.local.env; the local copy is ignored by Git.
#
# Required for paid-membership script builds: your 10-character Team ID.
# Personal Teams can be detected automatically after selecting the team in Xcode.
# See docs/BUILD_FROM_SOURCE.md for both paths.
TEAM_ID=ABCDEFGHIJ
#
# CONFIGURATION=Release
# DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer
